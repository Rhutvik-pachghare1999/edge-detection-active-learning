# AECS-SDC Tier-1 reproducible dependency pins.
# Install with:
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   python -m pytest tests/ -q
#
# PyTorch CUDA 13.0 wheels; other packages fall back to PyPI via --extra-index-url.
--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple

# Core ML / vision stack
torch==2.11.0
torchvision==0.26.0
ultralytics==8.4.41
transformers==5.6.2
accelerate==1.13.0
onnxruntime==1.25.0
onnxruntime-gpu==1.25.0
onnx==1.21.0

# Dataset / I/O
pycocotools==2.0.8
opencv-python==4.13.0.92
Pillow==12.2.0
numpy==2.4.4
PyYAML==6.0.3
requests==2.33.1
httpx==0.28.1

# Web / dashboard (legacy benchmark entry points still use these)
fastapi[standard]==0.136.1
uvicorn[standard]==0.46.0
websockets==16.0
python-multipart==0.0.26
aiofiles==25.1.0
streamlit==1.56.0
plotly==6.7.0
pandas==3.0.2
SQLAlchemy==2.0.49
aiosqlite==0.22.1

# Logging / config
loguru==0.7.3
python-dotenv==1.2.2

# Plotting
matplotlib==3.10.8

# Testing
pytest==9.1.1
